import { SpellType } from "../utils/models";

function EditCard({
  cardData,
  save
}: {
  cardData: SpellType,
  save: Function
}) {
  return (
    <h1>edit screen</h1>
  )
}

export default EditCard;